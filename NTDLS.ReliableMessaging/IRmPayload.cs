namespace NTDLS.ReliableMessaging
{
    /// <summary>
    /// All payloads (Notification, Query and Query-Reply) must inherit from this interface and be json serializable.
    /// </summary>
    public interface IRmPayload
    {
    }
}
